
      if (this.companyId) {
        localStorage.setItem('comp', this.companyId);

        // After companyId is confirmed, load the list with filters
        this.loadSeatOrderList(this.selectedStatus || 'tumu', this.selectedDate || 'sonbirhafta');
      } else {
        console.error('Hello.');
        console.error('Company ID not found.');
        console.error('Selamlar.')
      }


            if (this.companyId) {
        localStorage.setItem('comp', this.companyId);

        // After companyId is confirmed, load the list with filters
        this.loadSeatOrderList(this.selectedStatus || 'tumu', this.selectedDate || 'sonbirhafta');
      } else {
        console.error('Company ID not found.');
      }